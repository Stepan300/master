spring:
  datasource:
    url: jdbc:mysql://localhost:3306/todolist?useSSL=false&serverTimezone=UTC
    username: root
    password: ******
  jpa:
    hibernate:
      ddl-auto: none
forExample:
  value: for Example